#! /bin/bash

# run this from the main directory so that auto_file_sorter is a subdirectory in the current directory.

# pip install -e .
# The -e . option tells pip to install the package in the current directory in editable mode.

pip install .